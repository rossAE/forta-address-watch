"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g;
    return g = { next: verb(0), "throw": verb(1), "return": verb(2) }, typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (_) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.provideRunHandlersOnBlock = void 0;
var _1 = require(".");
function provideRunHandlersOnBlock(getAgentHandlers, getNetworkId, getBlockWithTransactions, getTransactionReceipt, getTraceData, createBlockEvent, createTransactionEvent) {
    _1.assertExists(getAgentHandlers, 'getAgentHandlers');
    _1.assertExists(getNetworkId, 'getNetworkId');
    _1.assertExists(getBlockWithTransactions, 'getBlockWithTransactions');
    _1.assertExists(getTransactionReceipt, 'getTransactionReceipt');
    _1.assertExists(getTraceData, 'getTraceData');
    _1.assertExists(createBlockEvent, 'createBlockEvent');
    _1.assertExists(createTransactionEvent, 'createTransactionEvent');
    return function runHandlersOnBlock(blockHashOrNumber) {
        return __awaiter(this, void 0, void 0, function () {
            var _a, handleBlock, handleTransaction, networkId, block, blockEvent, findings, traces, traceMap, _i, _b, transaction, receipt, txEvent, findings;
            return __generator(this, function (_c) {
                switch (_c.label) {
                    case 0: return [4, getAgentHandlers()];
                    case 1:
                        _a = _c.sent(), handleBlock = _a.handleBlock, handleTransaction = _a.handleTransaction;
                        if (!handleBlock && !handleTransaction) {
                            throw new Error("no block/transaction handler found");
                        }
                        console.log("fetching block " + blockHashOrNumber + "...");
                        return [4, getNetworkId()];
                    case 2:
                        networkId = _c.sent();
                        return [4, getBlockWithTransactions(blockHashOrNumber)];
                    case 3:
                        block = _c.sent();
                        if (!handleBlock) return [3, 5];
                        blockEvent = createBlockEvent(block, networkId);
                        return [4, handleBlock(blockEvent)];
                    case 4:
                        findings = _c.sent();
                        console.log(findings.length + " findings for block " + block.hash + " " + findings);
                        _c.label = 5;
                    case 5:
                        if (!handleTransaction)
                            return [2];
                        return [4, getTraceData(parseInt(block.number))];
                    case 6:
                        traces = _c.sent();
                        traceMap = {};
                        traces.forEach(function (trace) {
                            if (!trace.transactionHash)
                                return;
                            var txHash = trace.transactionHash.toLowerCase();
                            if (!traceMap[txHash])
                                traceMap[txHash] = [];
                            traceMap[txHash].push(trace);
                        });
                        _i = 0, _b = block.transactions;
                        _c.label = 7;
                    case 7:
                        if (!(_i < _b.length)) return [3, 11];
                        transaction = _b[_i];
                        return [4, getTransactionReceipt(transaction.hash)];
                    case 8:
                        receipt = _c.sent();
                        if (!receipt) {
                            console.log("error fetching receipt for " + transaction.hash);
                            return [3, 10];
                        }
                        txEvent = createTransactionEvent(receipt, block, networkId, traceMap[transaction.hash.toLowerCase()]);
                        return [4, handleTransaction(txEvent)];
                    case 9:
                        findings = _c.sent();
                        console.log(findings.length + " findings for transaction " + transaction.hash + " " + findings);
                        _c.label = 10;
                    case 10:
                        _i++;
                        return [3, 7];
                    case 11: return [2];
                }
            });
        });
    };
}
exports.provideRunHandlersOnBlock = provideRunHandlersOnBlock;
