import shelljs from "shelljs";
import prompts from "prompts";
export declare type UploadImage = () => Promise<string>;
export default function provideUploadImage(shell: typeof shelljs, prompt: typeof prompts, imageRepositoryUrl: string, imageRepositoryUsername: string, imageRepositoryPassword: string, agentName: string): UploadImage;
