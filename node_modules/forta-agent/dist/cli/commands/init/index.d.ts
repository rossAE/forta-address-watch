/// <reference types="node" />
import fs from 'fs';
import shelljs from 'shelljs';
import prompts from 'prompts';
import { CreateKeyfile } from '../../utils/create.keyfile';
import { CommandHandler } from '../..';
import { ListKeyfiles } from '../../utils/list.keyfiles';
export default function provideInit(shell: typeof shelljs, prompt: typeof prompts, filesystem: typeof fs, fortaKeystore: string, configFilename: string, listKeyfiles: ListKeyfiles, createKeyfile: CreateKeyfile): CommandHandler;
